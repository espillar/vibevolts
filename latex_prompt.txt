You are an expert in LaTeX and scientific writing. Your task is to generate a comprehensive LaTeX document that summarizes the VibeVolts project, a Python-based simulation toolkit for space environment modeling. Use the information provided below, which is sourced from the project's official documentation, to construct the document.

**Document Specifications:**

1.  **Document Class:** Use the `article` class with a font size of 11pt.
2.  **Required Packages:**
    *   `amsmath` for mathematical equations.
    *   `graphicx` for including images (though none are specified here).
    *   `geometry` with the option `margin=1in` for page layout.
    *   `hyperref` for clickable links.
    *   `minted` for syntax-highlighted code blocks (use the `python` language where appropriate).
    *   `tocloft` to customize the table of contents.
3.  **Title:** "VibeVolts: A Python-Based Toolkit for Space Environment Simulation"
4.  **Author:** "Jules, AI Software Engineer"
5.  **Date:** Use the current date.

**Document Structure and Content:**

**1. Preamble and Title Block:**
   - Set up the document class and import all the required packages.
   - Create the title, author, and date, and then generate the title block using `\maketitle`.

**2. Abstract:**
   - Write a concise summary of the VibeVolts project. It should mention that it is a Python-based toolkit for space environment modeling, its key features (orbit propagation, exclusion analysis, 3D visualization), and its modular, performance-oriented design using NumPy.

**3. Table of Contents:**
   - Add a table of contents using `\tableofcontents`.

**4. Section 1: Introduction:**
   - Expand on the abstract. Describe the purpose of VibeVolts: to provide a fast and efficient tool for simulating the visibility of space-based assets.
   - Mention its refactored, modular structure and its reliance on established astronomical libraries like `astropy`.
   - Briefly list the core capabilities: satellite propagation, celestial body positioning, viewing exclusion analysis, and 3D visualization.

**5. Section 2: Core Data Structures:**
   - This section should detail the main data structures used in the simulation.

   - **Subsection 2.1: The `simulation_data` Dictionary:**
     - Explain that this is the central, composable data structure that holds the entire simulation state.
     - Include a `minted` code block showing the structure of the dictionary, as documented in `vvdocs.md`.
     - Describe the key components: `counts`, `celestial`, `satellites`, `fixedpoints`, and `pointing_spheres`.

   - **Subsection 2.2: Radiometric and Physical Data:**
     - Briefly describe the `FILTER_DATA` dictionary from `radiometry_data.py`, explaining that it holds standard data for astronomical filters.
     - Mention the physical constants like `AU_M` and `RSUN_M`.

**6. Section 3: Core Modules and Algorithms:**
   - This section will describe the key Python modules and the algorithms they implement.

   - **Subsection 3.1: `simulation.py` - Simulation Setup:**
     - Describe the functions for initializing the simulation: `create_empty_simulation`, `add_celestial_bodies`, and `add_fixed_points`.

   - **Subsection 3.2: `propagation.py` - Orbit Dynamics:**
     - Explain the role of this module in handling the dynamics of the simulation.
     - Detail the `propagate_satellites_new` function, explaining that it uses a vectorized Keplerian propagator to update satellite positions.
     - Mention `celestial_update` for getting Sun and Moon positions via `astropy`.

   - **Subsection 3.3: `visibility.py` - Line-of-Sight Analysis:**
     - Describe the importance of this module for determining what a satellite can "see."
     - Explain the `exclusion` function, which checks for Sun, Moon, and Earth blockages.
     - Mention the vectorized `solarexclusion` function and the `update_visibility_table` function.

   - **Subsection 3.4: `pointing.py` - Satellite Pointing Control:**
     - Explain how this module manages where satellites are pointing.
     - Describe the functions for generating pointing grids (`generate_pointing_sphere`) and updating satellite pointing vectors (`update_satellite_pointing`).

**7. Section 4: Demonstrations and Usage:**
   - Explain that the toolkit includes a suite of demo scripts (`demo*.py`) to showcase its features.
   - List a few key demos and their purposes (e.g., `demo1` for basic propagation, `demo_exclusion_table` for visibility heatmaps, `demo_sky_scan` for mapping exclusion zones).
   - Include a `minted` code block showing how to run all demos using the `all_demos.py` script.

**8. Section 5: Dependencies:**
   - List the required Python libraries: `numpy`, `astropy`, `jplephem`, `sgp4`, `plotly`, `scipy`, and `ipython`.
   - Provide the `pip install` command in a `minted` block.

Please generate the complete LaTeX source code based on these instructions. Ensure the output is a single, valid `.tex` file.